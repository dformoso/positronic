---
description: Run the positronic audit-drift skill — sweep the project doc graph for drift (user-invoked).
---

Locate the `audit-drift` skill in your skills index (workspace `.agents/skills/audit-drift/SKILL.md`, else `~/.gemini/config/skills/audit-drift/SKILL.md`). Read the whole SKILL.md and execute it exactly, treating this as an explicit user invocation.
