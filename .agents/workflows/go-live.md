---
description: Run the positronic go-live skill — evidence GO/NO-GO gate before first traffic or a one-way cutover (user-invoked).
---

Locate the `go-live` skill in your skills index (workspace `.agents/skills/go-live/SKILL.md`, else `~/.gemini/config/skills/go-live/SKILL.md`). Read the whole SKILL.md and execute it exactly, treating this as an explicit user invocation.
