---
description: Run the positronic improve-readability skill — behavior-preserving comprehension pass over the codebase (user-invoked).
---

Locate the `improve-readability` skill in your skills index (workspace `.agents/skills/improve-readability/SKILL.md`, else `~/.gemini/config/skills/improve-readability/SKILL.md`). Read the whole SKILL.md and execute it exactly, treating this as an explicit user invocation.
