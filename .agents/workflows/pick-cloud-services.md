---
description: Run the positronic pick-cloud-services skill — pick an external/cloud service via live research and lock the decision record (user-invoked).
---

Locate the `pick-cloud-services` skill in your skills index (workspace `.agents/skills/pick-cloud-services/SKILL.md`, else `~/.gemini/config/skills/pick-cloud-services/SKILL.md`). Read the whole SKILL.md and execute it exactly, treating this as an explicit user invocation.
