---
description: Run the positronic readout skill — check weeks after launch whether the PRD's metrics moved and any kill criterion fired (user-invoked).
---

Locate the `readout` skill in your skills index (workspace `.agents/skills/readout/SKILL.md`, else `~/.gemini/config/skills/readout/SKILL.md`). Read the whole SKILL.md and execute it exactly, treating this as an explicit user invocation.
