---
description: Run the positronic run-wave skill — work one wave of issues in parallel, then reconcile the remaining backlog (user-invoked).
---

Locate the `run-wave` skill in your skills index (workspace `.agents/skills/run-wave/SKILL.md`, else `~/.gemini/config/skills/run-wave/SKILL.md`). Read the whole SKILL.md and execute it exactly, treating this as an explicit user invocation.
