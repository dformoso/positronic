---
description: Run the positronic to-frd skill — expand the PRD into per-feature behaviour, states, rules and acceptance criteria (user-invoked).
---

Locate the `to-frd` skill in your skills index (workspace `.agents/skills/to-frd/SKILL.md`, else `~/.gemini/config/skills/to-frd/SKILL.md`). Read the whole SKILL.md and execute it exactly, treating this as an explicit user invocation.
