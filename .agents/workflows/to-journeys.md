---
description: Run the positronic to-journeys skill — expand each PRD journey into an executable script with steps, branches and an end-state proof (user-invoked).
---

Locate the `to-journeys` skill in your skills index (workspace `.agents/skills/to-journeys/SKILL.md`, else `~/.gemini/config/skills/to-journeys/SKILL.md`). Read the whole SKILL.md and execute it exactly, treating this as an explicit user invocation.
