---
description: Run the positronic to-mockup skill — render every feature state as annotated screens in one self-contained HTML file (user-invoked).
---

Locate the `to-mockup` skill in your skills index (workspace `.agents/skills/to-mockup/SKILL.md`, else `~/.gemini/config/skills/to-mockup/SKILL.md`). Read the whole SKILL.md and execute it exactly, treating this as an explicit user invocation.
