---
description: Run the positronic to-mockups skill — pick the UI's identity and structure, then draw every journey step and branch as annotated screens (user-invoked).
---

Locate the `to-mockups` skill in your skills index (workspace `.agents/skills/to-mockups/SKILL.md`, else `~/.gemini/config/skills/to-mockups/SKILL.md`). Read the whole SKILL.md and execute it exactly, treating this as an explicit user invocation.
