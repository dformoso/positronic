---
description: Run the positronic to-prd skill — turn the current context into a versioned PRD (user-invoked).
---

Locate the `to-prd` skill in your skills index (workspace `.agents/skills/to-prd/SKILL.md`, else `~/.gemini/config/skills/to-prd/SKILL.md`). Read the whole SKILL.md and execute it exactly, treating this as an explicit user invocation.
