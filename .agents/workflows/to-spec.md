---
description: Run the positronic to-spec skill — turn the PRD + definitions into an implementation SPEC (user-invoked).
---

Locate the `to-spec` skill in your skills index (workspace `.agents/skills/to-spec/SKILL.md`, else `~/.gemini/config/skills/to-spec/SKILL.md`). Read the whole SKILL.md and execute it exactly, treating this as an explicit user invocation.
